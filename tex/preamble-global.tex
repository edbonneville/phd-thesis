% Only if sending to printers
\usepackage[
  cross,
  center,
  width=176mm,
  height=246mm
]{crop}

\KOMAoptions{%
  listof=totoc,
  listof=nochaptergap,
  listof=flat,
  index=totoc
}

% Try the crop ting for printing?

% For the chapter thumbs (!) Remove the actual label inside the box?
\usepackage{tex/chapterthumb}
\usepackage{scrlayer-scrpage}
\AddLayersToPageStyle{@everystyle@}{chapterthumb}
\addtokomafont{chapterthumb}{}

% Newly define commands common to many chapters
\DeclareMathOperator{\logit}{logit}
\DeclareMathOperator{\Var}{Var}
\DeclareMathOperator{\SE}{SE}
\newcommand{\given}{\,|\,}
\newcommand{\indep}{\perp\!\!\!\!\perp}
\newcommand\diff{\mathop{}\!\mathrm{d}}
\newcommand\Diff[1]{\mathop{}\!\mathrm{d^#1}}

% Bold and sans
\newcommand{\bsf}[1]{\textbf{\textsf{#1}}}

% Fancy chapter headings
%\include{tex/koma-chapter-headings}


%\usepackage{showframe}
